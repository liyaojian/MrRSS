# Dockerfile for building MrRSS server version
# This creates a minimal server binary without GUI dependencies

# Build stage
FROM golang:1.24-alpine AS builder

# Install git, Node.js, and build tools
# CGO is disabled for the server build as we use modernc.org/sqlite
RUN apk add --no-cache git nodejs npm

# Set working directory
WORKDIR /app

# Copy go mod files
COPY go.mod go.sum ./

# Download dependencies
RUN go mod download

# Copy source code
COPY . .

# Build frontend
RUN cd frontend && npm install && npm run build

# Build the server version - disable CGO for a portable static binary
RUN CGO_ENABLED=0 GOOS=linux go build -tags server -trimpath -buildvcs=false -ldflags="-w -s" -o mrrss-server ./main-core.go

# Runtime stage - minimal alpine image
FROM alpine:latest

# Install ca-certificates for HTTPS requests
RUN apk --no-cache add ca-certificates

# Create a non-root user
RUN adduser -D -s /bin/sh mrrss

# Create data directory with proper permissions
RUN mkdir -p /app/data && chown -R mrrss:mrrss /app/data

# Set working directory
WORKDIR /app

# Copy the binary from builder stage
COPY --from=builder /app/mrrss-server .

# Change ownership
RUN chown mrrss:mrrss mrrss-server

# Switch to non-root user
USER mrrss

# Expose port
EXPOSE 1234

# Health check
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
    CMD wget --no-verbose --tries=1 --spider http://localhost:1234/api/version || exit 1

# Run the server
CMD ["./mrrss-server", "-host", "0.0.0.0", "-port", "1234"]
